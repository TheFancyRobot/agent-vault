---
description: Validate Agent Vault integrity. Use when you need to check vault health, find broken links, missing frontmatter, or orphaned notes.
---

# Vault Validate

Validate the Agent Vault integrity.

Call the `vault_validate` tool to run all integrity validators:
- Frontmatter shape and required keys
- Required headings and generated-block balance
- Required links between notes
- Orphan detection

Fix any errors before continuing work.
